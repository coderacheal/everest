<%= form_with(model: expense, url: category_expense_path(@category, expense), local: true) do |form| %>
  <% if expense.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
        <% expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :name, class: "form-input"%>
  </div>

  <div>
    <%= form.number_field :amount, class: "form-input" %>
  </div>

  <div class="submission-btn-div">
    <%= link_to "Go Back", request.referrer || root_path, class: 'submit-button' %>
    <%= form.submit class: "submit-button" %>
  </div>
  
<% end %>
